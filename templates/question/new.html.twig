{% extends 'base.html.twig' %}

{% block title %}{{'Create new Question'|trans}}{% endblock %}

{% block body %}
    <h1>{{'Create new Question'|trans}}</h1>

    {{ include('question/_form.html.twig') }}
        <a class="btn btn-primary" href="{{ path('question_index') }}">{{'back to list'|trans}}</a>
{% endblock %}
